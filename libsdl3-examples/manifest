: 1
name: libsdl3-examples
version: 3.4.12-a.0.z
type: exe
language: c
project: sdl
summary: cross-platform multimedia C library (examples)
license: other: public domain ; Released into the public domain.
description-file: README.md
package-description-file: PACKAGE-README.md
changes-file: WhatsNew.txt
url: https://www.libsdl.org/
doc-url: https://wiki.libsdl.org/
src-url: https://github.com/libsdl-org/SDL
email: slouken@libsdl.org
package-url: https://github.com/build2-packaging/SDL
package-email: packaging@build2.org ; Mailing list.
depends: * build2 >= 0.18.0
depends: * bpkg >= 0.18.0

# CI configuration (keep aligned with libsdl3).
#
# Examples build on the same platform matrix as the library and the test
# suite; they only ever link against SDL3.
#

builds: -freebsd
builds: -macos
builds: -windows
builds: -bindist

macos-clang-builds: macos
macos-clang-build-include: macos_*-clang**
macos-clang-build-exclude: **

windows-msvc-builds: windows
windows-msvc-build-include: windows*-msvc**
windows-msvc-build-exclude: **

# Debian/Fedora sys: package installs are disabled for the time being:
# bpkg's on-demand sys-install can misfire on CI (observed: apt removing
# the bot's own linux-image kernel package while resolving these
# graphics/audio libs). See https://github.com/build2/build2/issues/555.
# Re-enable once resolved upstream. The package lists below are otherwise
# unchanged and should still be correct.
#
#debian-build-config:
#\
#sys:libx11
#sys:libxext
#sys:libxcursor
#sys:libxi
#sys:libxfixes
#sys:libxrandr
#sys:libxss
#sys:libxtst
#sys:libxkbcommon
#sys:libgl
#sys:libasound2
#sys:libpulse
#sys:libpipewire-0.3
#sys:libdrm
#sys:libgbm
#sys:libudev
#sys:libdbus-1
#\
#debian-builds: sys
#debian-builds: -static
#debian-builds: -bindist
#debian-build-include: linux_debian*-**
#debian-build-exclude: **

# Fedora/RHEL-alike: same intent, see ../libsdl3/manifest for why
# -bindist is subtracted below.
#
#fedora-build-config:
#\
#sys:libX11
#sys:libXext
#sys:libXcursor
#sys:libXi
#sys:libXfixes
#sys:libXrandr
#sys:libXScrnSaver
#sys:libXtst
#sys:libxkbcommon
#sys:libdrm
#sys:libusb1
#\
#fedora-builds: sys
#fedora-builds: -static
#fedora-builds: -bindist
#fedora-build-include: linux_fedora*-**
#fedora-build-exclude: **
